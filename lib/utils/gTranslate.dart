// import 'package:localize_and_translate/localize_and_translate.dart';
//
// Future<String> gTranslate(String inputKey) async {
//   String translatedText = await translator.translateWithGoogle(
//       key: inputKey,
//       from: translator.currentLanguage,
//       to: translator.currentLanguage == 'en' ? 'ar' : ' en');
//   return translatedText;
// }
